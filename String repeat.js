// Creat Empty Str
// Take The Number Argument And Loop Through
// Add The String To The Empty One
//

function repeatStr(num, str) {
  // let newStr = "";
  // for (i = 0; i < num; i++) {
  //   newStr += str;
  // }
  // return newStr;

  return str.repeat(num);
}

console.log(repeatStr(5, "lvu\n"));
